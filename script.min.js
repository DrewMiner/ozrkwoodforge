function showNextSlide(){slides[currentSlide].style.opacity="0",currentSlide=(currentSlide+1)%slides.length,slides[currentSlide].style.opacity="1"}const slides=document.querySelectorAll(".hero-slideshow img");let currentSlide=0;setInterval(showNextSlide,4e3),document.querySelectorAll(".faq-question").forEach(e=>{e.addEventListener("click",()=>{const l=e.nextElementSibling;l.style.display="block"===l.style.display?"none":"block"})});