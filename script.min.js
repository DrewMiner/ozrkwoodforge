function showNextSlide(){slides[currentSlide].style.opacity="0",currentSlide=(currentSlide+1)%slides.length,slides[currentSlide].style.opacity="1"}const slides=document.querySelectorAll(".hero-slideshow img");let currentSlide=0;setInterval(showNextSlide,4e3),document.querySelectorAll(".faq-question").forEach(e=>{e.addEventListener("click",()=>{const t=e.nextElementSibling;t.style.display="block"===t.style.display?"none":"block"})}),fetch("/blog.json").then(e=>e.json()).then(e=>{let t=document.getElementById("blog-section");t.innerHTML="",e.forEach(e=>{let l=document.createElement("div");l.classList.add("blog-post");let n=e.image?`<img src="${e.image}" alt="${e.title}">`:"",o=e.excerpt?e.excerpt:e.content.substring(0,100)+"...";l.innerHTML=`\n                ${n}\n                <h3>${e.title}</h3>\n                <p><small>${e.date}</small></p>\n                <p>${o}</p>\n                <a href="blog-post.html?post=${e.slug}" class="read-more">Read More</a>\n            `,t.appendChild(l)})}).catch(e=>console.log("Error loading blog posts:",e));let mainBackButton=document.getElementById("back-button");mainBackButton&&(mainBackButton.style.opacity="1");let headerBackButton=document.getElementById("header-back-button");headerBackButton&&(headerBackButton.style.opacity="1");

